# APEX GATE frontend — variabili d'ambiente (opzionali)
# Copia in .env.local se devi sovrascrivere i default.

# URL del backend/proxy usato DIRETTAMENTE dalla pagina Chat (bypassa il proxy Vite).
# Default se non impostata: http://localhost:8000
VITE_PROXY_BASE_URL=http://localhost:8000
